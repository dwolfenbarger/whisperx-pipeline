# Build-tool constraints for reliable pip isolated builds on Python 3.12 (Windows included).
# openai-whisper is built from source and imports pkg_resources during setup.
# Keep setuptools below the release line where pkg_resources was removed.
setuptools>=70,<81

# Ensure wheel is available in isolated build envs.
wheel>=0.44,<1
